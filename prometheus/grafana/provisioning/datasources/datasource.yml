apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # `prometheus` is the compose service name; Grafana resolves it on the
    # shared `monitoring` network.
    url: http://prometheus:9090
    isDefault: true
    editable: true